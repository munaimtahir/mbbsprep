# Legacy Tests (Archived)

`tests/legacy/` contains historical verification scripts and ad-hoc tests retained for context only.

Canonical automated tests are under `tests/automated/`.

Do not use legacy tests as current quality gates without revalidation.
