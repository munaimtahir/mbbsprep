// Shared MCQ Management URLs and Configuration
window.staffUrls = {
    getTopicsAjax: '', // Will be set from template
    questionToggleStatus: '', // Will be set from template
    questionDelete: '', // Will be set from template
    questionBulkAction: '' // Will be set from template
};

// Initialize from template data
document.addEventListener('DOMContentLoaded', function() {
    // URLs will be set by individual templates
    // This file provides the shared namespace
});
