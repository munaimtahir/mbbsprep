# Legacy Scripts (Archived)

`scripts/legacy/` contains one-off debug/demo/fix utilities retained as historical context.

Operational scripts intended for recurring use remain in `scripts/`.

Do not treat legacy scripts as canonical automation.
