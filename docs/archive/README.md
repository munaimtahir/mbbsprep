# Historical / archived documentation

Everything in this folder is retained for historical context only.

It may describe older fixes, temporary verification passes, or implementation plans that no longer reflect the verified v1 baseline. Use the active docs under `docs/00_governance`, `docs/10_contracts`, `docs/20_workflows`, and `docs/30_repo` instead.
